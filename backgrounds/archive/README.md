# Earlier 120-frame backgrounds

These sequences are retained for earlier experiments. They are not included in
the active library dropdown or automatic selection. To use one, select its custom
path under `backgrounds/archive/` in the GUI. Earlier experiment configurations
that named these files at the top level need this updated path to reuse them.

[Original inventory](INVENTORY.md) · [Original metadata](library.json)
