# Python 3.12 recommended; supported versions: >=3.12,<3.14.
# Run from the repository root: python -m pip install -r requirements.txt
# Direct dependencies pinned to the versions in uv.lock at release preparation.
# Transitive dependencies are resolved by pip; this is not a complete lockfile.
# FFmpeg with libx264 support must be installed separately for MP4 export.
# Background observations and detector weights must be downloaded separately.

# Scientific model and visualization
h5py==3.16.0
matplotlib==3.11.1
numpy==2.5.1
scikit-image==0.26.0
scipy==1.18.0
threadpoolctl==3.6.0
tqdm==4.69.1

# Filament detector
timm==1.0.28
torch==2.9.1
torchvision==0.24.1
transformers==4.57.6

# Notebook execution
ipykernel==6.31.0
nbclient==0.11.0
nbformat==5.11.1

# Local experiment GUI
streamlit==1.63.0
tomli-w==1.2.0

# Install the local package so scripts can import synthetic_filaments.
# Keep pyproject.toml alongside this file; pip uses it without requiring uv.
-e .
